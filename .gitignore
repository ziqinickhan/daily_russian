# Xcode - build artifacts
build/
DerivedData/

# Xcode - user-specific (never commit these at any depth)
**/xcuserdata/
**/xcshareddata/IDEWorkspaceChecks.plist

# Xcode - breakpoints
**/xcdebugger/

# Swift Package Manager
.swiftpm/
Package.resolved

# CocoaPods (if we ever use it)
Pods/

# Secrets & config — real keys stay local
*.xcconfig
!Sample.xcconfig
**/Secrets/deepseek_config.json
Config/Debug.xcconfig
Config/Release.xcconfig

# macOS
.DS_Store
*.swp
*~

# Core Data - the .xcdatamodeld IS committed (it's source code)

# Fastlane / archive
fastlane/report.xml
*.ipa
*.dSYM.zip
*.dSYM
